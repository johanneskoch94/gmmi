# Get template variable

get_template_variables returns the template variable names and
definitions.

## Usage

``` r
get_template_variables(return_only_names = FALSE)
```

## Arguments

- return_only_names:

  If TRUE (default set to FALSE), return vector of variable names.

## Value

Data-frame with the reporting template variables
