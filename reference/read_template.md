# Read-in and tidy the reporting template.

read_template reads-in the template file (shipped with the package), and
performs minor operations to tidy up the data.

## Usage

``` r
read_template(sheet = "Baseline template")
```

## Arguments

- sheet:

  Name or number of sheet to read in.

## Value

Tidy data-frame with the reporting template
